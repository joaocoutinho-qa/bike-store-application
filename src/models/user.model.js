
class User {
  constructor({ id, username, password, role, nome }) {
    this.id = id;
    this.username = username;
    this.password = password;
    this.role = role;
    this.nome = nome;
  }
}

export default User;
